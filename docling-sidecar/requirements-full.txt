# Full local/container install — base + docling rich parser.
# Use for local dev or when deploying to a container host that can hold
# the ~2GB footprint (Render, Fly.io, Cloud Run, Modal, etc.).
-r requirements.txt
docling>=2.0
